import React from "react";
// Todo: what is the standard to write component name, currently its smallcase

const Workflow = () => {
    return <div> hi this is WorkFlow component</div>
}

export default Workflow